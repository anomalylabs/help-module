<aside>
    <h4>{{ trans('anomaly.module.help::message.related_sections') }}</h4>

    <ul class="list-unstyled">
        {% for section in section.category.sections.except(section.id) %}
            <li>
                <a href="{{ section.route('view') }}">
                    {{ section.name }}
                </a>
            </li>
        {% endfor %}
    </ul>
</aside>
